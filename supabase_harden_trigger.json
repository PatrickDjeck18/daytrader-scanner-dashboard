{
  "project_id": "nafzdjyehhuexjsipaeb",
  "name": "harden_updated_at_trigger",
  "query": "create or replace function public.set_updated_at() returns trigger language plpgsql set search_path = '' as $$ begin new.\"updatedAt\" = pg_catalog.now(); return new; end; $$;"
}
